/* Q- Valid parenthesis --> some validity checking parameter like depth?--> dp constraint?
      All possible combos --> recursion + backtrack if invalid combo is being formed
      //backtracking but how to form all possible pairs
      1)by keeping at one given time two choice
      2)by first performing one choice and then performing the second 
      ---> need to choose one out of these two approaches to build on the ans
      lexicographical order --> sort? ya store while generating in such a way that formed and stored in lexico order on its own
      backtracking needed?? -> yes buidling from scratch once again every time is going to consume so much of more unecessary computational time for us
*/
